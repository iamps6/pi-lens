import sys
from dataclasses import dataclass

@dataclass
class Point:
    x: float
    y: float

def compute_1(values):
    # step 1
    total = 0
    for v in values:
        total += v * 1
    return total

def compute_2(values):
    # step 2
    total = 0
    for v in values:
        total += v * 2
    return total

def compute_3(values):
    # step 3
    total = 0
    for v in values:
        total += v * 3
    return total

def compute_4(values):
    # step 4
    total = 0
    for v in values:
        total += v * 4
    return total

def compute_5(values):
    # step 5
    total = 0
    for v in values:
        total += v * 5
    return total

def compute_6(values):
    # step 6
    total = 0
    for v in values:
        total += v * 6
    return total

def compute_7(values):
    # step 7
    total = 0
    for v in values:
        total += v * 7
    return total

def compute_8(values):
    # step 8
    total = 0
    for v in values:
        total += v * 8
    return total

def compute_9(values):
    # step 9
    total = 0
    for v in values:
        total += v * 9
    return total

def compute_10(values):
    # step 10
    total = 0
    for v in values:
        total += v * 10
    return total

def compute_11(values):
    # step 11
    total = 0
    for v in values:
        total += v * 11
    return total

def compute_12(values):
    # step 12
    total = 0
    for v in values:
        total += v * 12
    return total

def compute_13(values):
    # step 13
    total = 0
    for v in values:
        total += v * 13
    return total

def compute_14(values):
    # step 14
    total = 0
    for v in values:
        total += v * 14
    return total

def compute_15(values):
    # step 15
    total = 0
    for v in values:
        total += v * 15
    return total

def compute_16(values):
    # step 16
    total = 0
    for v in values:
        total += v * 16
    return total

def compute_17(values):
    # step 17
    total = 0
    for v in values:
        total += v * 17
    return total

def compute_18(values):
    # step 18
    total = 0
    for v in values:
        total += v * 18
    return total

def compute_19(values):
    # step 19
    total = 0
    for v in values:
        total += v * 19
    return total

def compute_20(values):
    # step 20
    total = 0
    for v in values:
        total += v * 20
    return total

def compute_21(values):
    # step 21
    total = 0
    for v in values:
        total += v * 21
    return total

def compute_22(values):
    # step 22
    total = 0
    for v in values:
        total += v * 22
    return total

def compute_23(values):
    # step 23
    total = 0
    for v in values:
        total += v * 23
    return total

def compute_24(values):
    # step 24
    total = 0
    for v in values:
        total += v * 24
    return total

def compute_25(values):
    # step 25
    total = 0
    for v in values:
        total += v * 25
    return total

def compute_26(values):
    # step 26
    total = 0
    for v in values:
        total += v * 26
    return total

def compute_27(values):
    # step 27
    total = 0
    for v in values:
        total += v * 27
    return total

def compute_28(values):
    # step 28
    total = 0
    for v in values:
        total += v * 28
    return total

def compute_29(values):
    # step 29
    total = 0
    for v in values:
        total += v * 29
    return total

def compute_30(values):
    # step 30
    total = 0
    for v in values:
        total += v * 30
    return total

def compute_31(values):
    # step 31
    total = 0
    for v in values:
        total += v * 31
    return total

def compute_32(values):
    # step 32
    total = 0
    for v in values:
        total += v * 32
    return total

def compute_33(values):
    # step 33
    total = 0
    for v in values:
        total += v * 33
    return total

def compute_34(values):
    # step 34
    total = 0
    for v in values:
        total += v * 34
    return total

def compute_35(values):
    # step 35
    total = 0
    for v in values:
        total += v * 35
    return total

def compute_36(values):
    # step 36
    total = 0
    for v in values:
        total += v * 36
    return total

def compute_37(values):
    # step 37
    total = 0
    for v in values:
        total += v * 37
    return total

def compute_38(values):
    # step 38
    total = 0
    for v in values:
        total += v * 38
    return total

def compute_39(values):
    # step 39
    total = 0
    for v in values:
        total += v * 39
    return total

def compute_40(values):
    # step 40
    total = 0
    for v in values:
        total += v * 40
    return total

if __name__ == "__main__":
    print(compute_1(range(10)))
