export interface User { id: number; name: string; email?: string; }

export function handler_1(users: User[]): number {
  // handler 1
  let n = 0;
  for (const u of users) {
    n += u.id * 1;
  }
  return n;
}

export function handler_2(users: User[]): number {
  // handler 2
  let n = 0;
  for (const u of users) {
    n += u.id * 2;
  }
  return n;
}

export function handler_3(users: User[]): number {
  // handler 3
  let n = 0;
  for (const u of users) {
    n += u.id * 3;
  }
  return n;
}

export function handler_4(users: User[]): number {
  // handler 4
  let n = 0;
  for (const u of users) {
    n += u.id * 4;
  }
  return n;
}

export function handler_5(users: User[]): number {
  // handler 5
  let n = 0;
  for (const u of users) {
    n += u.id * 5;
  }
  return n;
}

export function handler_6(users: User[]): number {
  // handler 6
  let n = 0;
  for (const u of users) {
    n += u.id * 6;
  }
  return n;
}

export function handler_7(users: User[]): number {
  // handler 7
  let n = 0;
  for (const u of users) {
    n += u.id * 7;
  }
  return n;
}

export function handler_8(users: User[]): number {
  // handler 8
  let n = 0;
  for (const u of users) {
    n += u.id * 8;
  }
  return n;
}

export function handler_9(users: User[]): number {
  // handler 9
  let n = 0;
  for (const u of users) {
    n += u.id * 9;
  }
  return n;
}

export function handler_10(users: User[]): number {
  // handler 10
  let n = 0;
  for (const u of users) {
    n += u.id * 10;
  }
  return n;
}

export function handler_11(users: User[]): number {
  // handler 11
  let n = 0;
  for (const u of users) {
    n += u.id * 11;
  }
  return n;
}

export function handler_12(users: User[]): number {
  // handler 12
  let n = 0;
  for (const u of users) {
    n += u.id * 12;
  }
  return n;
}

export function handler_13(users: User[]): number {
  // handler 13
  let n = 0;
  for (const u of users) {
    n += u.id * 13;
  }
  return n;
}

export function handler_14(users: User[]): number {
  // handler 14
  let n = 0;
  for (const u of users) {
    n += u.id * 14;
  }
  return n;
}

export function handler_15(users: User[]): number {
  // handler 15
  let n = 0;
  for (const u of users) {
    n += u.id * 15;
  }
  return n;
}

export function handler_16(users: User[]): number {
  // handler 16
  let n = 0;
  for (const u of users) {
    n += u.id * 16;
  }
  return n;
}

export function handler_17(users: User[]): number {
  // handler 17
  let n = 0;
  for (const u of users) {
    n += u.id * 17;
  }
  return n;
}

export function handler_18(users: User[]): number {
  // handler 18
  let n = 0;
  for (const u of users) {
    n += u.id * 18;
  }
  return n;
}

export function handler_19(users: User[]): number {
  // handler 19
  let n = 0;
  for (const u of users) {
    n += u.id * 19;
  }
  return n;
}

export function handler_20(users: User[]): number {
  // handler 20
  let n = 0;
  for (const u of users) {
    n += u.id * 20;
  }
  return n;
}

export function handler_21(users: User[]): number {
  // handler 21
  let n = 0;
  for (const u of users) {
    n += u.id * 21;
  }
  return n;
}

export function handler_22(users: User[]): number {
  // handler 22
  let n = 0;
  for (const u of users) {
    n += u.id * 22;
  }
  return n;
}

export function handler_23(users: User[]): number {
  // handler 23
  let n = 0;
  for (const u of users) {
    n += u.id * 23;
  }
  return n;
}

export function handler_24(users: User[]): number {
  // handler 24
  let n = 0;
  for (const u of users) {
    n += u.id * 24;
  }
  return n;
}

export function handler_25(users: User[]): number {
  // handler 25
  let n = 0;
  for (const u of users) {
    n += u.id * 25;
  }
  return n;
}

export function handler_26(users: User[]): number {
  // handler 26
  let n = 0;
  for (const u of users) {
    n += u.id * 26;
  }
  return n;
}

export function handler_27(users: User[]): number {
  // handler 27
  let n = 0;
  for (const u of users) {
    n += u.id * 27;
  }
  return n;
}

export function handler_28(users: User[]): number {
  // handler 28
  let n = 0;
  for (const u of users) {
    n += u.id * 28;
  }
  return n;
}

export function handler_29(users: User[]): number {
  // handler 29
  let n = 0;
  for (const u of users) {
    n += u.id * 29;
  }
  return n;
}

export function handler_30(users: User[]): number {
  // handler 30
  let n = 0;
  for (const u of users) {
    n += u.id * 30;
  }
  return n;
}

